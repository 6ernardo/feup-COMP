import io.som;
class Foo {

   public static void main(String[] args) {
   }

   public int somCalls(){
      int temp1;
      int temp2;
      som s;

      // virtual calls
      s = new som();
      temp1 = s.som1(1,2);
      temp1 = s.som1(temp1+9,2);

      // static calls

      temp1 = som.som2(1,2);
      temp2 = som.som2(temp1+9,2);

      return temp1;
   }

   public Foo getInstance(){
      Foo tipo;
      int res;

      tipo = new Foo();

      // call an unknown method
      //res = tipo.unknown(); // should throw an error

      // call a known method
      res = tipo.somCalls();

      res = this.somCalls();
      return tipo;
   }

}